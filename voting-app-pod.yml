apiVersion: v1
kind: Pod
metadata:
  name: voting-app-pod
  labels:
    name: voting-app-pod
    app: voting-app
spec:
  containers:
  - name: voting-app
    image: dhanvantarisoftware/vote-image
    ports:
         - containerPort: 80
